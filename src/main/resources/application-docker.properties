# Docker database configuration (app and Postgres on the same Docker network)
spring.datasource.url=jdbc:postgresql://postgres:5432/inventory_db
spring.datasource.username=inventory_user
spring.datasource.password=inventory_password
